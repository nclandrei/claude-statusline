[package]
name = "claude-statusline"
version = "0.1.0"
edition = "2021"
description = "Minimal statusline renderer for Claude Code"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
opt-level = "z"
lto = true
strip = true
